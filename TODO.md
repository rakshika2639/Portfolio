# TODO - Achievement section embed fix

- [x] Update achievement.html to use LinkedIn embed that matches provided iframe snippet (urn without collapsed param)
- [x] Replace broken cross-origin fallback logic with simple deterministic rendering (show iframe + hide fallback after successful iframe load; otherwise show fallback)

- [x] Add a custom static fallback card using the provided photo/info if iframe fails (includes image, description text, and link)

- [x] Verify no duplicate closing tags and that page renders without JS errors



